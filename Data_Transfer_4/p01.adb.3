--with  Gnat.IO;
with Text_Io;

procedure P01 is 
   --use Ada.Text_IO;
   use Text_Io;

   
   ------------------------------------------------------------------------------
      task R is
      entry A (data : in integer);
            --entry B (data : out integer);
            
   end R;


   task body R is
      L: integer;      
   begin
            L:=0;    
            loop        
      accept A (data : in integer) do
          Put_Line("T call R");
          L:= L + data;
          Put_Line("Put L into R from T, L = " & Integer'Image(L));
       end A;
               end loop;
   end R;
   ------------------
   
      task type T is
      --entry A (from_t_to_r : in integer);
            --entry B (data : out integer);
            
   end T ;


   task body T is
      L: integer;
   begin
      L :=100;
      R.A (L);
      
   end T ;
      
      T_1 : T;
      T_2 : T;
  ----------------------  
  

   
   
begin
    
   Put_Line("Hello, world!");
   
      

end P01;